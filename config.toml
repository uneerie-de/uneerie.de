# This is a simple configuration.
# If you want to see the full configuration, please check `full-config.toml` .
# And see https://gohugo.io/getting-started/configuration/ .

baseURL = "/"

title = "uneerie"

enableRobotsTXT = true
enableEmoji = true

theme = "jane"


hasCJKLanguage = false                # has chinese/japanese/korean?
paginate = 10                         # Number of articles displayed on the homepage
rssLimit = 20                         # Limit Entry Count to Rss file
disqusShortname = ""                  # disqus_shortname
googleAnalytics = ""                  # UA-XXXXXXXX-X
copyright = "&#169; uneerie"          # default: author.name

# language support
defaultContentLanguage = "de"

[languages.de]
  languageCode = "de"

[author]
  name = "uneerie"
  url = "/about"

[sitemap]
  changefreq = "weekly"
  priority = 0.5
  filename = "sitemap.xml"

[[menu.main]]
  name = "Home"
  weight = 10
  identifier = "home"
  pageref = "/"

[[menu.main]]
  name = "Tags"
  weight = 30
  identifier = "tags"
  pageref = "/tags/"

[[menu.main]]
  name = "Categories"
  weight = 40
  identifier = "categories"
  pageref = "/categories/"


[params]
  since = "2002"            # Site creation time
  homeFullContent = true    # if false, show post summaries on home page. Otherwise show full content.
  rssFullContent = true     # if false, Rss feed instead of the summary

  # site info (optional)
  logoTitle = "uneerie"

  keywords = ["Hugo", "theme", "jane"]
  description = "Hugo theme jane example site."

  # The date format to use; for a list of valid formats, see https://gohugo.io/functions/format/
  dateFormatToUse = "02.01.2006"

  # Some global options, you can also close or open something in front matter for a single post, see more information from `archetypes/default.md`.
  toc = true
  photoswipe = true         # see https://github.com/dimsemenov/PhotoSwipe
  contentCopyright = '<a rel="license noopener" href="https://creativecommons.org/licenses/by-nc-nd/4.0/" target="_blank">CC BY-NC-ND 4.0</a>'

  # Link custom CSS and JS assets
  #   (relative to /static/css and /static/js respectively)
  customCSS = []            # if ['custom.css'], load '/static/css/custom.css' file
  customJS = []             # if ['custom.js'], load '/static/js/custom.js' file

  [params.social]
  # a-email = "mailto:netti@uneerie.de"
  # m-instagram = "@uneerie_" -> TODO (er erwartet einen Link und nicht nur den Benutzernamen)
